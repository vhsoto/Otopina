
			<tr>
				<td><%= event.Detail %></td>
				<td class="pull-right"><%= link_to edit_event_category_event_path(event.event_category,event), class: "btn btn-info btn-sm" do %>
						<i class="fa fa-pencil"></i>
					<% end %>
				</td>
				<td  class="pull-right"><%= link_to event_category_event_path(event.event_category,event), method: :delete, data: { confirm: '¿Está seguro(a) que quiere eliminar?' }, class: "btn btn-danger btn-sm" do %>
                <i class="fa fa-trash"></i>  
              <% end %>  </td>
			</tr>
		

