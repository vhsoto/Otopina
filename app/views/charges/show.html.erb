<% provide(:title, "Cargos") %>
<h1>Cargo</h1>
<br>
<%= link_to 'Atrás', charges_path, class: "btn btn-primary btn-sm" %>
<br>
<br>
<div class="table-responsive">
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Nombre</th>
				<th colspan="3"></th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td><%= @charge.name %></td>
				<td><%= link_to edit_charge_path(@charge), class: "btn btn-info btn-sm pull-right" do %>
	                	<i class="fa fa-pencil"></i>
	              	<% end %>
          		</td>
			</tr>
		</tbody>
	</table>
</div>
<br>
<%= link_to 'Atrás', charges_path, class: "btn btn-primary btn-sm" %>
