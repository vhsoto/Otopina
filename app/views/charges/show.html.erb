<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @charge.name %>
</p>

<%= link_to 'Edit', edit_charge_path(@charge) %> |
<%= link_to 'Back', charges_path, class: "btn btn-primary btn-sm pull-right" %>
